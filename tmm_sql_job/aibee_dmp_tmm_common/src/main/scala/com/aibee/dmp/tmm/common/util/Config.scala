package com.aibee.dmp.tmm.common.util

object Config {
  val JOB_INFO = "job_id,job_type,job_name,job_stream_name,job_desc,job_group_name,pre_job,creator"
  val SQL_JOB_INFO = "source_type,source_name,udf_name,udf_jar_name,udf_jar_main_clazz,sql_body,pre_sql,after_sql,sql_args"


  // 加密解密所需key
  val KEY = "bA0%tU1^cI2%"
}
